framework:
    asset_mapper:
        # The paths to make available to the asset mapper.
        paths:
            - assets/
            - public/assets/
        missing_import_mode: ignore

when@prod:
    framework:
        asset_mapper:
            missing_import_mode: warn
