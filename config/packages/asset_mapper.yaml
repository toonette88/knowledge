framework:
    asset_mapper:
        public_directory: '%kernel.project_dir%/public'
        cache_dir: '%kernel.cache_dir%/assets'
        manifest_filename: 'manifest.json'


when@prod:
    framework:
        asset_mapper:
            missing_import_mode: warn
