{% extends 'base.html.twig' %}

{% block title %}Mon Profil{% endblock %}

{% block body %}
    <div class="container">
        <h1>Bienvenue sur votre profil, {{ app.user.name }} {{ app.user.firstname }} !</h1>
        
        <div class="list-group">
            <a href="{{ path('user_informations') }}" class="list-group-item list-group-item-action">
                Mes informations
            </a>
            <a href="{{ path('user_lessons') }}" class="list-group-item list-group-item-action">
                Mes cours
            </a>
            <a href="{{ path('user_invoices') }}" class="list-group-item list-group-item-action">
                Mes factures
            </a>
            <a href="{{ path('profile_course') }}" class="list-group-item list-group-item-action">
                Mes certifications
            </a>
        </div>
    </div>
{% endblock %}
